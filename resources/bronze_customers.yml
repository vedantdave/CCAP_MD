resources:
  jobs:
    bronze_customer:
      name: ${bundle_target}-bronze-customers
      tasks:
        - task_key: ingest_customer
          notebook_task:
            notebook_path: ../02_bronze/01_create_customers.ipynb
          environment_key: default
      environments:
        - environment_key: default
          spec:
            client: "2"
            dependencies:
              - "pyspark"